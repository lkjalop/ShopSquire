__pycache__/
*.py[cod]
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

.env
.env.*

tmp/
.tmp_test_reports/
.tmp_pytest_*.log
.tmp_*.log
.tmp_*.txt
.tmp_*.db

# Local runtime artifacts / recordings (keep only the compact summary JSON)
runs/**
!runs/docker_pytest_results.json

# Local dumps, logs, and DB volumes (do not commit)
dump/
logs/
pgdata/

# Local scripted run outputs (keep scripts, not their outputs)
scripts/runs/

# One-off local collection artifacts
collected_*

# Local model weights (download separately as needed)
/yolov8*.pt
/models/*.pt

# Playbook version snapshots can be noisy in dev; keep curated ones only.
config/security/versions/playbooks/*.json

# Risk correlation policy snapshots are local artifacts (frequently regenerated)
config/security/versions/risk_correlation_policy_*.json

*.sqlite
*.db
*.sqlite-journal
/*.sqlite
/*.db
test.sqlite*

node_modules/
dist/
build/

.venv/
venv/
.venv_new/

# Editor / tool local state
.claude/settings.local.json

# Local test log captures (run artifacts, never source)
.testlogs/
.tmp_pw_repro*.py
.codex_fullsuite.ps1

# Local Docker/backup artifacts
compose.effective.yml
*.bundle

# C05: Backup files + encrypted backups — never commit database artifacts
*.backup
*.sql.gz
*.sql.enc
*.dump
config/tls/certs/

# Runtime server logs emitted at repo root during dev/test runs
backend-*.log
backend-*.err.log
backend-*.out.log
frontend-*.log
frontend-*.err.log
frontend-*.out.log
debug.log

# Temporary probe/keeper scripts and their outputs (generated during dev sessions)
tmp_*.log
tmp_*.js
tmp_*.py
tmp_*.json
tmp_*.png
tmp_*.txt
tmp_pdf_extracts/
tmp_ui_uploads/
tmp_server_keeper.*
tmp_keeper_*.log
vid-demo/

# Backup / scratch files
*.bak_utf8
*.baktest
_tmp_write_test.txt

# Playwright MCP runtime state
.playwright-mcp/

# Resume / personal files accidentally placed in repo root
resume_*.md

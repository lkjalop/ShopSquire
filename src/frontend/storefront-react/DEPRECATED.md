This storefront is deprecated.

Use `frontend/` as the single supported storefront codepath.

Migration notes:
- Supported app: `frontend/src/App.tsx`
- Legacy app here is kept only for historical reference.
- Do not add new features or fixes under `src/frontend/storefront-react/`.

"""Rules-first (Tier0) gates and deterministic policy checks.

These modules are intentionally dependency-light so they can run before any
heavy ML/LLM calls and can be reused across vertical packs.
"""


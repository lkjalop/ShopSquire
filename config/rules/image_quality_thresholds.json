{
  "min_bytes": 8000,
  "min_dim": 128,
  "min_quality_score": 0.6,
  "allowed_formats": ["jpeg", "png", "webp"],
  "notes": "Optional config referenced by the roadmap; Tier0 uses VerticalPack.thresholds.image_quality_min at runtime."
}


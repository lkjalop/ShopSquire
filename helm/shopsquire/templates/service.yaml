apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopsquire.fullname" . }}
  labels:
    {{- include "shopsquire.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "shopsquire.selectorLabels" . | nindent 4 }}
